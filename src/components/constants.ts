export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password'
}

export enum ButtonColor {
  ROSE = 'btn-rose',
  GREEN = 'btn-green',
  GREY = 'btn-grey'
}

export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset'
}
